import { RequestsortPipe } from './requestsort.pipe';

describe('RequestsortPipe', () => {
  it('create an instance', () => {
    const pipe = new RequestsortPipe();
    expect(pipe).toBeTruthy();
  });
});
